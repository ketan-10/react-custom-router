import React, { useContext } from "react";

export default React.createContext("");



// useContext(Context); // ==== > for accessing the value
// Context.Provider  /// =====> setting the value


// export default Context;